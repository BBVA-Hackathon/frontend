import { MapContainer, ListMapContainer } from './style';

const ListMap = () => {
  return (
    <MapContainer>
      <ListMapContainer>Lista de Bancos</ListMapContainer>
      <div> map</div>
    </MapContainer>
  );
};
export default ListMap;
